
# Generated with configureplus (CONFIGUREPLUS_VERSION=0.16.2, CONFIGUREPLUS_PWD=/home/muenalan/git-workdirs/btest/build/platforms/linux-gnu)
# Note: space before a comment are part of a variable

CONFIGURE_MKTEMP=$(shell cat .configureplus/global/CONFIGURE/MKTEMP)
CONFIGURE_PKGNAME=$(shell cat .configureplus/global/CONFIGURE/PKGNAME)
CONFIGURE_OSTYPE=$(shell cat .configureplus/global/CONFIGURE/OSTYPE)
CONFIGURE_BASH_PROFILE_FILE=$(shell cat .configureplus/global/CONFIGURE/BASH_PROFILE_FILE)
CONFIGURE_TIMESTAMP=$(shell cat .configureplus/global/CONFIGURE/TIMESTAMP)
CONFIGURE_FLAG_TOOL_BTEST=$(shell cat .configureplus/global/CONFIGURE/FLAG_TOOL_BTEST)
CONFIGUREPLUS_DIR_CONFIG=$(shell cat .configureplus/global/CONFIGUREPLUS/DIR_CONFIG)
CONFIGUREPLUS_SESSION=$(shell cat .configureplus/global/CONFIGUREPLUS/SESSION)
CONFIGURE_PKGNAME=$(shell cat .configureplus/global/CONFIGURE_PKGNAME)
